同适配器的字面含义，适配器设计模式是接受所拥有的接口，产生所需要的接口。

```java
/**
 * @FilePath interfaces.demo.ch9_3.interfaceprocessor/FilterProcessor.java
 * @Author
 * @Date 1/6/2020 4:09 PM
 * @Version 1.0.0
 * @Description
 */

package interfaces.demo.ch9_3.interfaceprocessor;

import interfaces.demo.ch9_3.filters.*;

class FilterAdapter implements Processor {
    Filter filter;
    public FilterAdapter(Filter filter) {
        this.filter = filter;
    }
    public String name() { return filter.name(); }
    public Waveform process(Object input) {
        return filter.process((Waveform)input);
    }
}

public class FilterProcessor {
    public static void main(String[] args) {
        Waveform w = new Waveform();
        Apply.process(new FilterAdapter(new LowPass(1.0)), w);
        Apply.process(new FilterAdapter(new HighPass(2.0)), w);
        Apply.process(
                new FilterAdapter(new BandPass(3.0, 4.0)), w);
    }
}/* FilterProcessor output
Using Processor LowPass
Waveform 0
Using Processor HighPass
Waveform 0
Using Processor BandPass
Waveform 0
 *///:~
```

