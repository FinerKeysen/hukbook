组合：将对象引用置于新类中即可。

示例：

```java
//: reusing/SprinklerSystem.java
// Composition for code reuse.

class WaterSource {
  private String s;
  WaterSource() {
    System.out.println("WaterSource()");
    s = "Constructed";
  }
  public String toString() { return s; }
}	

public class SprinklerSystem {
  private String valve1, valve2, valve3, valve4;
  private WaterSource source = new WaterSource();
  private int i;
  private float f;
  public String toString() {
    return
      "valve1 = " + valve1 + " " +
      "valve2 = " + valve2 + " " +
      "valve3 = " + valve3 + " " +
      "valve4 = " + valve4 + "\n" +
      "i = " + i + " " + "f = " + f + " " +
      "source = " + source;
  }	
  public static void main(String[] args) {
    SprinklerSystem sprinklers = new SprinklerSystem();
    System.out.println(sprinklers);
  }
} /* Output:
WaterSource()
valve1 = null valve2 = null valve3 = null valve4 = null
i = 0 f = 0.0 source = Constructed
*///:~
```

类中的域为基本类型时能够自动被初始化为零。但是对象引用会被初始化为null，如果通过null的对象调用其任何方法都会得到一个异常。在不抛出异常时，可以打印一个null引用。

如果要初始化这些引用，可以通过以下集中方式：

1、在定义对象的地方，也即是在构造器被调用前被初始化

2、在类的构造器中

3、在正要使用这些对象之前，该方式称为：惰性初始化。在生成对象不值得以及不必每次都生成对象的情况下，该方式可以减少额外的负担

4、使用实例初始化

示例：

```java
/**
 * reusing/Bath.java
 * Author: Think in Java
 * Date:   2019/8/17
 * Descriptions: 类中的域为基本类型时能够自动被初始化为零。
 * 但对象引用会被初始化为null，如果试图为它们调用任何方法，都会得到一个异常——运行时错误
 * 如果项初始化这些引用，可以在以下位置进行：
 *   1、在定义对象的地方，也即是在构造器被调用前被初始化
 *   2、在类的构造器中
 *   3、在正要使用这些对象之前，该方式称为Delayed initialization。在生成对象不值得
 *   以及不必每次都生成对象的情况下，该方式可减少额外的负担
 *   4、使用实例初始化
 */

package reusing;

class Soap {
    private String s;
    Soap() {
        System.out.println("Soap()");
        s = "Constructed.";
    }
    public String tiString() {
        return s;
    }
}
public class Bath {
    private String
            s1 = "Hell0", // 方式1、在定义处初始化
            s2, s3;
    private Soap soap;
    private int i;
    private float f;

    Bath() { // 方式2、在构造函数中初始化
        s2 = "Java";
        f = 3.14f;
        soap = new Soap();
    }

    {
        i = 100; // 方式3、实例初始化
    }

    public String toString() {
        if(s3 == null) { // 方式3、delayed initialization
            s3 = "Hello, Java.";
        }
        return
            "s1 = " + s1 + "\n" +
            "s2 = " + s2 + "\n" +
            "s3 = " + s3 + "\n" +
            "i = " + i + "\n" +
            "f = " + f + "\n" +
            "soap = " + soap;
    }
    public static void main(String[] args) {
        Bath b = new Bath();
        System.out.println(b);
    }
}/* output
Soap()
s1 = Hell0
s2 = Java
s3 = Hello, Java.
i = 100
f = 3.14
soap = reusing.Soap@1b6d3586
*///:~
```

> 练习：创建一个简单类，在第二个类中，将一个引用定义为第一个类的对象，运用惰性初始化来实例化这个对象

```java
/**
 * Trees.java
 * Author:   HK
 * Date:     2019/8/1715:42
 * Description: 创建一个简单类，在第二个类中，将一个引用定义为第一个类的对象，
 * 运用惰性初始化来实例化这个对象
 */

package reusing;

class Leave {
    private String s;
    Leave() {
        System.out.println("Leave()");
        s = "Constructed.";
    }
    public String toString() {
        return s;
    }
}
public class Trees {
    private int i;
    private float f;
    private String s1;
    private Leave leave;

    Trees() {
        System.out.println("Inside Trees()");
    }
    public String toString() {
        if(leave == null)
            leave = new Leave();;
        return
            "i = " + i + "\n" +
            "f = " + f + "\n" +
            "s1 = " + s1 + "\n" +
            "leave = " + leave;
    }
    public static void main(String[] args) {
        Trees tree = new Trees();
        System.out.println(tree);
    }
} /* outpit
Inside Trees()
Leave()
i = 0
f = 0.0
s1 = null
leave = Constructed.
*///:~
```

